{
  "version": 1,
  "applications": [
    {
      "hostname": "zeaz.dev",
      "paths": ["/", "/v1/*"],
      "defaultDecision": "deny",
      "policies": [
        "explicit-allowed-users",
        "explicit-cli-service-auth"
      ],
      "identityProvider": "configure-in-cloudflare-zero-trust",
      "sessionDuration": "4h",
      "originAuthorization": "application JWT and tenant-scoped RBAC"
    }
  ],
  "notes": [
    "Replace the identity provider placeholder before enabling Cloudflare Access.",
    "Do not use Everyone or All valid emails in an allow policy.",
    "Do not store Access service tokens in this file."
  ]
}
