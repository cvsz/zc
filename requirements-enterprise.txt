# Backward-compatible dependency entry point.
#
# The canonical standalone API requirements live in requirements-deploy.in.
# Keep this file for existing developer workflows without reintroducing the
# retired NATS, PostgreSQL, HTTP/3, or hosted-platform architecture.

-r requirements-deploy.in
